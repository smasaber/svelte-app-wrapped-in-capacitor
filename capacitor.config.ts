import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.svelte.app.wrapped.in.capacitor',
  appName: 'svelte-app-wrapped-in-capacitor',
  webDir: 'চুদিরবাই'
};

export default config;
